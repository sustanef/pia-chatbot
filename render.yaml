services:
  - type: web
    name: pia-chatbot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run pia_chatbot_app.py --server.port $PORT --server.address 0.0.0.0"
    plan: free
